*/ Definicion de Variables /*
DIM [ limite, resultador_final, prediccion ] AS [ integer, real, integer ]
DIM [ contador, acumulador ] AS [ integer, integer ]
DIM [mensaje_final] AS [ string ]

*/ Inicio de Programa /*
contador := 0
acumulador := 1

DISPLAY "Inserte el Limite"
GET limite

DISPLAY "Introduzca una prediccion"
GET prediccion

DISPLAY "Introduzca un mensaje final"
GET mensaje_final

WHILE(acumulador < 1000 AND contador < 10){
    acumulador := acumulador * contador
    contador := contador + 1
    IF(acumulador >= limite){
        DISPLAY "Se supero el limite"
        DISPLAY acumulador
        resultado_final := acumulador * contador + limite
    } ELSE {
        DISPLAY "Debajo del limite"
    }
}

IF (EQUMAX(resultado_final - 15, [100, 110, prediccion])) {
    DISPLAY "Acertaste Maximo"
}

IF (EQUMIN(resultado_final + 10, [100, 110, prediccion])) {
    DISPLAY "Acertaste Minimo"
}

IF (LONG([100, 110, prediccion]) == resultado_final) {
    DISPLAY "Longitud"
}

DISPLAY mensaje_final
